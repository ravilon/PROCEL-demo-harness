# Local broker started by docker compose up -d
PROCEL_MQTT_HOST=localhost
PROCEL_MQTT_PORT=1883
PROCEL_MQTT_USERNAME=
PROCEL_MQTT_PASSWORD=
PROCEL_MQTT_TLS=false

# Required by the HTTP publisher; choose a long random value.
PUBLISH_API_KEY=
HTTP_PORT=3000
